Developing
==========

Community contributions to Jaxion are welcome!

Start by forking the repository on `GitHub <https://github.com/JaxionProject/jaxion>`_, creating a new branch, and making a pull request with your changes.

Check out the `CONTRIBUTING <https://github.com/JaxionProject/jaxion/blob/main/CONTRIBUTING.md>`_ guidelines.

Code changes should pass testing (each commit is automatically tested with GitHub Actions).
Tests can be run locally in the project directory with ``pytest``:

.. code-block:: bash

    pytest

Tests are located in the ``jaxion/tests/`` directory. New features should include appropriate tests.
